jdk:
  - openjdk8

before_install:
  - cp build-forge.gradle build.gradle
  - chmod +x gradlew

install:
  - ./gradlew clean setupDecompWorkspace --refresh-dependencies --no-daemon

after_success:
  - ./gradlew build publishToMavenLocal --no-daemon
